# .env.example — Copy to .env.local and fill values
# For local dev, leave VITE_API_URL empty (Vite proxy handles it)
VITE_API_URL=

# For production (Vercel), set to your deployed backend URL:
# VITE_API_URL=https://pdf-nexus-backend.onrender.com
